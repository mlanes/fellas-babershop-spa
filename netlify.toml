[build]
  command = "npm run build"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "20"

# Nuxt 3 handles its own routing via Nitro server
# No redirects needed - Netlify Functions will handle SSR
