# @nuxtjs/storybook@9.0.1 declares a peer of storybook@~9.0.5 but its own
# deps require storybook@^9.1.x — contradictory metadata. legacy-peer-deps
# lets `npm install` resolve it. Remove once @nuxtjs/storybook ships a
# fixed stable release aligned with Storybook 9.1.
legacy-peer-deps=true